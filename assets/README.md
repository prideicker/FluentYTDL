Place your application icon here as `logo.png` (PNG, suggested 256x256). Use `python scripts/install_icon.py path\to\your_icon.png` to copy it into this folder.

The app will load `assets/logo.png` as its tray and window icon if present.