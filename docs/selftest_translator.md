# 错误翻译器离线自测

目标：在不依赖真实 YouTube 网络环境的情况下，验证 `translate_error()` 的主要分支能稳定产出结构化错误对象。

## 运行方式

在项目根目录执行：

```bash
python scripts/selftest_translator.py
```

若看到每个 case 输出 `OK`，说明翻译器核心分支正常。

## 覆盖范围（除“链接无效/不受支持”外）
- 403/风控/需要登录
- 网络超时/网络不可达/连接失败
- 视频不可用/私享
- FFmpeg 缺失/后处理失败

> “链接无效/不受支持”按你的要求不纳入本次自测。
